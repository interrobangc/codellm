export const VECTOR_DB_COLLECTIONS = {
  fileSummary: 'fileSummary',
} as const;

export const VECTOR_DBS = {
  chromadb: 'chromadb',
} as const;

export const VECTOR_DB_MODULES = {
  chromadb: './db/chromadb/index.js',
} as const;
