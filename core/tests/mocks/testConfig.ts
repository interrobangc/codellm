import type { PartialConfig } from '@/.';

export const testConfig: PartialConfig = {
  project: {
    name: 'testProject',
  },
  paths: {
    cache: './.cache',
    project: './testProject',
  },
};
