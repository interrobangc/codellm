export const IMPORTER_ERRORS = {
  'importer:import': {
    message: 'Error running import method for tool',
  },
  'importer:noTools': {
    message: 'No tools were found to import with',
  },
} as const;
